@model IEnumerable<DVDRental.Models.MembershipCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Membership Categories</h1>

<p>
    <a asp-action="Create" class="btn btn-success"><i class="bi bi-plus-circle"></i>Add New</a>
</p>
<table class="table table-primary table-hover float-center" style="width:auto">
    <thead class="table-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MembershipCategoryDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MembershipCategoryTotalLoans)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MembershipCategoryDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MembershipCategoryTotalLoans)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MembershipCategoryNumber" class="btn btn-warning">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MembershipCategoryNumber" class="btn btn-secondary">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MembershipCategoryNumber" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
