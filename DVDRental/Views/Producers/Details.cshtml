@model DVDRental.Models.Producer

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Producer</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ProducerName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ProducerName)
        </dd>
    </dl>
</div>
<div>
    <a class=" btn btn-primary" asp-action="Edit" asp-route-id="@Model?.ProducerNumber">Edit</a> |
    <a class="btn btn-secondary" asp-action="Index"><i class="bi bi-backspace"></i>Back to List</a>
</div>
